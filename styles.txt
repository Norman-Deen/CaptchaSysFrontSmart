/* 🟢 تحريك حدود مربع الاختبار الحقيقي (real-checkbox) لإعطاء تأثير متغير */
/* 🔹 السبب: لجعل المربع الحقيقي يبدو مختلفًا قليلاً عن البقية بطريقة غير واضحة تمامًا، 
   مما قد يساعد المستخدم البشري في التمييز بينه وبين المربعات الوهمية دون أن يكون الفرق واضحًا جدًا للروبوتات. */
   @keyframes realCheckboxAnimation {
    0% { border-color: black; } /* يبدأ باللون الأسود */
    50% { border-color: #444; } /* يتحول إلى لون رمادي أغمق */
    100% { border-color: black; } /* يعود إلى اللون الأسود */
}

/* 🟢 تحريك حدود المربعات الوهمية (fake-checkboxes) لتغيير لونها بشكل دوري */
/* 🔹 السبب: تضليل الروبوتات! الروبوتات غالبًا تعتمد على تحليل الأنماط الثابتة. 
   إذا كانت جميع المربعات تبدو متشابهة طوال الوقت، فقد يسهل عليها اكتشاف `real-checkbox` عن طريق تحليل CSS. 
   ولكن عندما تتغير الحدود بشكل متكرر، يصبح من الصعب عليها التعرف على المربع الصحيح بناءً على المظهر فقط. */
@keyframes fakeCheckboxAnimation {
    0% { border-color: white; } /* تبدأ باللون الأبيض */
    50% { border-color: #ff0000; } /* تتغير إلى اللون الأحمر */
    100% { border-color: white; } /* تعود إلى الأبيض */
}

/* 🔵 تصميم الصفحة الأساسية */
body {
    display: flex;
    justify-content: center; /* محاذاة المحتوى في المنتصف أفقيًا */
    align-items: center; /* محاذاة المحتوى في المنتصف عموديًا */
    height: 100vh; /* جعل المحتوى يمتد على كامل ارتفاع الشاشة */
    background-color: #f4f4f4 !important; /* لون خلفية محايد */
    font-family: Arial, sans-serif;
    user-select: none !important; /* منع المستخدم من تحديد النص، لمنع أي سلوك غير مرغوب */
}

/* 🔵 صندوق الاختبار */
.captcha-box {
    display: flex;
    flex-direction: column; /* ترتيب العناصر عموديًا */
    align-items: center; /* محاذاة العناصر في المنتصف */
    background: white;
    padding: 20px;
    border-radius: 5px;
    border: 2px solid #d3d3d3; /* إطار رمادي فاتح */
    width: 350px; /* تحديد عرض مناسب */
    text-align: center;
}

/* 🔵 حاوية المربعات */
#checkbox-container {
    position: relative;
    display: flex;
    gap: 15px; /* مسافة بين المربعات */
    margin-bottom: 10px;
}

/* 🔵 إعدادات المربعات */
input[type="checkbox"] {
    appearance: none; /* ✅ إزالة النمط الافتراضي للمتصفح */
    width: 25px;
    height: 25px;
    cursor: pointer;
    border: 2px solid white; /* ✅ جميع المربعات تبدأ بحدود بيضاء */
    background-color: white;
    box-shadow: none !important;
    transition: border-color 0.5s ease-in-out; /* ✅ تغيير لون الحدود بسلاسة */
}

/* ✅ `real-checkbox` يتغير باستمرار */
/* 🔹 السبب: لجعله متحركًا قليلاً بشكل لا يلاحظه الروبوت بسهولة، لكنه يمكن أن يكون إشارة خفيفة للمستخدم الحقيقي. */
#real-checkbox {
    animation: realCheckboxAnimation 2s infinite alternate; /* ✅ تغيير مستمر */
}

/* ✅ `hidden-checkboxes` تتغير باستمرار ولكن بتأخير */
/* 🔹 السبب: جعل كل مربع وهمي يتغير بوقت مختلف، مما يزيد من تعقيد عملية التعرف عليه من قبل الروبوتات. */
.hidden-checkbox {
    animation: fakeCheckboxAnimation 2s infinite alternate;
    animation-delay: calc(0.2s * var(--delay)); /* ✅ تأخير زمني عشوائي لكل مربع */
}



/*  عرض الرسائل للمستخدم */
.captcha-message {
    margin-top: 10px;
    padding: 10px;
    border-radius: 6px;
    font-weight: bold;
    font-size: 0.9rem;
    max-width: 300px;
  }
  
  .captcha-message.info {
    color: #0c5460;
    background-color: #d1ecf1;
  }
  
  .captcha-message.warning {
    color: #856404;
    background-color: #fff3cd;
  }
  
  .captcha-message.success {
    color: #155724;
    background-color: #d4edda;
  }
  
  .captcha-message.error {
    color: #721c24;
    background-color: #f8d7da;
  }