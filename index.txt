ممتاز! خلينا نبدأ بتوثيق هذا الجزء من الواجهة الأمامية (Frontend) بشكل احترافي ومنظم باللغة العربية ✍️

---

# 📄 index.html – التوثيق الكامل للواجهة الأمامية لنظام CAPTCHA

## ✅ الغرض من الملف
`index.html` هو الملف الأساسي الذي يُحمّل عند زيارة صفحة CAPTCHA.  
يتضمن هذا الملف **العناصر الأساسية** لعرض واجهة المستخدم والتفاعل معها، مثل:

- المربعات (checkboxes)
- منطقة الاختبار
- ربط ملفات CSS و JavaScript اللازمة للعمل

---

## ✅ مكونات الملف

### 1️⃣ `<!DOCTYPE html>`
يُعرّف هذا السطر نوع المستند على أنه HTML5 – وهذا ضروري لضمان عرض الصفحة بشكل سليم في المتصفحات الحديثة.

---

### 2️⃣ `<html lang="en">`
يعني أن لغة المحتوى داخل الصفحة هي الإنجليزية. يمكن تغييره إلى `"ar"` إذا أردنا جعلها عربية بالكامل.

---

### 3️⃣ `<head>`
يحتوي على معلومات غير مرئية للمستخدم لكنها مهمة للمتصفح، وتشمل:

#### 🔹 `<meta charset="UTF-8">`
يحدد أن ترميز الأحرف المستخدم هو UTF-8، وهذا ضروري لدعم اللغات المختلفة مثل العربية والإنجليزية.

#### 🔹 `<meta name="viewport"...>`
يسمح للصفحة بالتكيف مع شاشات الهواتف والأجهزة المختلفة (Responsive Design).

#### 🔹 `<title>CaptchaSys</title>`
عنوان الصفحة الذي يظهر في تبويب المتصفح.

#### 🔹 `<link rel="stylesheet" href="styles.css">`
يربط ملف التنسيق الخارجي `styles.css` لتطبيق التنسيقات على العناصر داخل الصفحة.

---

### 4️⃣ `<body>`

#### 🔸 `<div class="captcha-box">`
مربع ملون (باللون السماوي) يحتوي على منطقة اختبار CAPTCHA.

#### 🔸 `id="checkbox-container"`
عنصر فارغ مبدئيًا، يتم ملؤه بالمربعات (checkboxes) ديناميكيًا من خلال كود JavaScript (`script.js`).

---

### 5️⃣ `<script src="script.js"></script>`
يربط ملف JavaScript المسؤول عن:
- إنشاء مربعات الاختيار تلقائيًا.
- تحليل حركة الماوس.
- جمع البيانات السلوكية.
- إرسالها إلى السيرفر عبر `fetch()`.

---

## ✅ الهدف العام
هذا الملف هو الواجهة التي يتفاعل معها المستخدم، وكل التنسيقات والتفاعلات تحدث عن طريق:
- `styles.css` ← الشكل والمظهر
- `script.js` ← المنطق والسلوك

---

هل تود أن نكمل الآن توثيق ملف `styles.css` أو `script.js`؟  
أو أحب أجهز لك هذا التوثيق بصيغة `.txt` أو `.md`؟